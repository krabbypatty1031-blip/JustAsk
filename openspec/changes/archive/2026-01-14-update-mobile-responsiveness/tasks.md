## 1. Implementation
- [ ] 1.1 Update `frontend/index.html` with `viewport-fit=cover` meta tag.
- [ ] 1.2 Define CSS variables for safe area insets in `frontend/src/style.css`.
- [ ] 1.3 Update `frontend/src/App.vue` layout container to respect safe area insets (padding-top/bottom).
- [ ] 1.4 Update bottom navigation in `frontend/src/App.vue` to handle bottom safe area (home indicator).
- [ ] 1.5 Review and update global CSS in `frontend/src/style.css` for mobile-first defaults (max-width constraints, flex wrapping).
- [ ] 1.6 Verify and adjust specific views (`Login.vue`, `Register.vue`, `Home.vue`, etc.) for narrow screen widths if broken.
