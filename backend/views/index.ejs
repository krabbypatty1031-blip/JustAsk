<!DOCTYPE html>
<html lang="zh-HK">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fs-2 fw-bold" href="/">JustAsk</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <% if (user) { %>
            <li class="nav-item me-3">
              <span class="navbar-text fs-4 text-white">您好，<strong><%= user.username %></strong></span>
            </li>
            <li class="nav-item">
              <a class="btn btn-outline-light btn-lg" href="/users/logout">退出登錄</a>
            </li>
          <% } else { %>
            <li class="nav-item me-2">
              <a class="btn btn-outline-light btn-lg" href="/users/login">登錄</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-primary btn-lg" href="/users/register">註冊帳號</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container mt-5">
    <div class="p-5 mb-4 bg-white rounded-3 shadow-sm">
      <div class="container-fluid py-5">
        <h1 class="display-4 fw-bold">歡迎來到 JustAsk 問答社區</h1>
        <p class="col-md-8 fs-3">這是一個專門為您設計的問答平台。在這裡，您可以放心提問，也可以把您的寶贵經驗分享給他人。</p>
        <% if (user) { %>
          <button class="btn btn-primary btn-lg px-5 py-3 fs-3" type="button">我要提問</button>
        <% } else { %>
          <a href="/users/register" class="btn btn-primary btn-lg px-5 py-3 fs-3">加入我們</a>
        <% } %>
      </div>
    </div>
  </main>
</body>
</html>